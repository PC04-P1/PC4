@model dynamic

@{
	ViewData["Title"] = "Producto";
}

<div class="row">
	<div class="col-md-4">
		<h3 class="titulo">Productos </h3>
	</div>
</div>

<div class="input-group mb-3">
	<form asp-controller="TipoProducto" asp-action="Producto">
	<select class="custom-select btn btn-outline-light" type="button"  name="BuscarProducto" onchange="this.form.submit()">
		<option ><i class="fas fa-leaf"></i>Tipo de Producto</option>
		@foreach (TipoProducto item in Model.TipoProducto)
		{
			<option class="dropdown-item" value="@item.ID">@item.Nombre</option>
		}
	</select>
	</form>
</div>



<div class="container">

			@foreach (Producto producto in Model.Producto)
			{
				@if (producto.Deshabilitado==false)
				{
				<a asp-controller="Producto" asp-action="Detalle" asp-route-id="@producto.ID">
				<button type="submit" class="btn btn-light">
				<div style="width: 18rem;">
					<img src="@Url.Content(producto.ImagenURL)" class="card-img-top center-cropped">
					<div class="card-body">
						<h5 class="card-tittle" align="center">@producto.Nombre</h5>
						<!-- Al Carrito -->
						<a href="#" align="center" class="btn btn-outline-primary">PEN @producto.Precio.ToString()</a>
					</div>
				</div>
				</button>
				</a>
				}
			}
 </div>
